# so
 Developed a code in C that simulates a complete OS. It consists of 5 modules: Kernel (with an integrated shell); CPU; RAM; I/O's (keyboard, monitor, etc). Each module ran in separate computers and uses TCP/IP protocol for communication. This was a group assignment for my university. 
